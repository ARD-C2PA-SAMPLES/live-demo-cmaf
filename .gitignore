# Local configuration, holds the Unified Streaming license key
# (see .env.example) - never commit it
.env

# Trusted Media (C2PA) key material and AIX documents, generated by
# generate-certs-and-aix.sh - never commit private keys
*.pem
*.srl
*.aix
*.ext

# macOS
.DS_Store
